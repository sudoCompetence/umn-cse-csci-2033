		/* enum type */
		enum day = {sun, mon, tue, wed, thu, fri, sat};
		enum direction = {north=0, south = 180, east = 90, west = 270};
		enum months = { jan = 1, feb, mar, apr, may, jun, jul, aug, sep, oct, nov, dec};

		/* enum use */
		enum months curr_month = mar;